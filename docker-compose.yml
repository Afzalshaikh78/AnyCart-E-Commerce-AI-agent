services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: anycart
      POSTGRES_USER: anycart
      POSTGRES_PASSWORD: anycart
    ports:
      - "5433:5432"
    volumes:
      - anycart_postgres:/var/lib/postgresql/data

volumes:
  anycart_postgres: